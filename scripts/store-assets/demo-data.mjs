/**
 * Chiffres de démonstration affichés dans les captures.
 *
 * Volontairement fictifs : une fiche Chrome Web Store est du matériel public,
 * on n'y met ni vrai pseudo ni vraie vignette de stream.
 *
 * Ce fichier portait aussi un jeu de streamers de démonstration, avec ses
 * générateurs d'avatars et de vignettes en SVG. Rien ne l'importait : les
 * chaînes des captures viennent de `scripts/dev/mock-chrome.js`, qui les
 * fabrique lui-même. Le tout a été retiré plutôt que maintenu en double.
 */

export const DEMO_STATS = {
  points: 12480,
  watchTimeHours: 9,
  watchTimeMinutes: 28,
  liveCount: 2,
};
